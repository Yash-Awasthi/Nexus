// SPDX-License-Identifier: Apache-2.0
// Contacts — placeholder page
// Real contacts data comes from apps/api once an integration is wired
export default function ContactsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold mb-2">Contacts</h1>
      <p className="text-muted-foreground text-sm">
        Contact management will be available once the connector is configured.
      </p>
    </div>
  );
}
