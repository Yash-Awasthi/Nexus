{
  "name": "@nexus/cli",
  "version": "0.0.0",
  "private": true,
  "description": "npx nexus CLI — submit objectives, manage approvals, inspect queues",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.6.3",
    "tsx": "^4.19.1",
    "vitest": "^2.1.8",
    "@types/node": "^22.0.0"
  },
  "bin": {
    "nexus": "./dist/index.js"
  },
  "dependencies": {
    "@nexus/contracts": "workspace:*",
    "@nexus/runtime": "workspace:*",
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "ora": "^8.1.0"
  }
}
