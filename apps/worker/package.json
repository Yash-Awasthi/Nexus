{
  "name": "@nexus/worker",
  "version": "0.0.0",
  "private": true,
  "description": "BullMQ queue consumers — signal pipeline, task execution",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "clean": "rm -rf dist coverage"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.1",
    "@types/node": "^22.0.0"
  },
  "dependencies": {
    "@nexus/contracts": "workspace:*",
    "@nexus/council": "workspace:*",
    "@nexus/db": "workspace:*",
    "@nexus/governance": "workspace:*",
    "@nexus/shared": "workspace:*",
    "bullmq": "^5.14.0",
    "ioredis": "^5.4.1",
    "drizzle-orm": "^0.45.2"
  }
}
