# Quarantine

Do not add speculative architecture trees, synthetic audit reports, or unverified metrics here.

Ship only code and docs that are tested under `npm test` and `npm run healthcheck`. For architecture and product intent, see `docs/` and `README.md`.
