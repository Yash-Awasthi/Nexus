{
  "name": "@nexus/context-pruning",
  "version": "0.1.0",
  "description": "Dynamic conversation context management — prune, compress, and optimize token usage",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
