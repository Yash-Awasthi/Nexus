{
  "name": "@nexus/billing",
  "version": "0.1.0",
  "private": true,
  "description": "API key management, quota enforcement, and Stripe webhook handler",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "@types/node": "^22.0.0",
    "fastify": "^5.9.0"
  },
  "dependencies": {
    "@nexus/db": "workspace:*",
    "@nexus/provider-registry": "workspace:*",
    "drizzle-orm": "^0.45.2"
  }
}
