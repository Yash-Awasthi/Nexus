{
  "name": "@nexus/rate-limiter",
  "version": "0.1.0",
  "description": "Multi-dimensional sliding window rate limiting with tiered endpoints, lockout, and per-tool plugin bindings",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
