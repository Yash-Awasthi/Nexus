{
  "name": "@nexus/llm-translate",
  "version": "0.0.0",
  "private": false,
  "description": "Lossless hub-and-spoke translation between LLM API formats (OpenAI chat ↔ Anthropic Messages). Canonical internal form gives any↔any with O(N) adapters; tool calls and multi-turn structure are preserved.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
