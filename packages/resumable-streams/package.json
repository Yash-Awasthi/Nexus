{
  "name": "@nexus/resumable-streams",
  "version": "0.1.0",
  "description": "Resumable SSE streaming — reconnect to in-flight streams after page refresh",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
