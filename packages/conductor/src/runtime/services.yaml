# GhostStack Services Configuration
# Defines service types and ports for health monitoring.

services:
  floci:
    type: docker
    port: 4566
  fcc:
    type: process
    port: 8082
  mcp:
    type: process
    port: 8000
    cmd: "python runtime/mcp/conductor_mcp_server.py"
