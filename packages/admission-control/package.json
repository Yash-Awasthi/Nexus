{
  "name": "@nexus/admission-control",
  "version": "0.1.0",
  "description": "Queue-based request admission with capacity management — prevents overload by rejecting before body read",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
