{
  "name": "@nexus/mixture-of-agents",
  "version": "0.1.0",
  "description": "Layered proposer/aggregator architecture for multi-agent synthesis — each layer refines output quality",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
