{
  "name": "@nexus/complexity-router",
  "version": "0.1.0",
  "description": "Complexity-based prompt routing — classify prompts and route to local or cloud models for cost optimization",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
