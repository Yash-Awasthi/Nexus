{
  "name": "@nexus/session-summarizer",
  "version": "0.0.0",
  "private": false,
  "description": "Session auto-summarizer — on session.end, distill conversation to key decisions and facts via LLM, persist to memory",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" } },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": { "typescript": "^5.6.3", "vitest": "^2.1.8" }
}
